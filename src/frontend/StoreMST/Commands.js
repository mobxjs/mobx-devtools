const prefix = 'frontend-mobx-react';

export const SET_BACKEND_STATE = `${prefix}:set-backend-state`;
export const APPEND_SNAPSHOTS = `${prefix}:append-snapshots`;
export const APPEND_PATCHES = `${prefix}:append_patches`;
export const APPEND_ACTIONS = `${prefix}:append_actions`;


